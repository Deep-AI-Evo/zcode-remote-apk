<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/web_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background" />

    <ProgressBar
        android:id="@+id/progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:indeterminate="true"
        android:visibility="gone" />

    <!-- 浮动工具条：返回 / 刷新 / 更多 / 收起；可拖动，默认顶部中间 -->
    <LinearLayout
        android:id="@+id/float_bar"
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:layout_gravity="top|center_horizontal"
        android:background="@drawable/bg_float_bar"
        android:elevation="6dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/add_back"
            android:scaleType="centerInside"
            android:src="@drawable/ic_arrow_back_small" />

        <ImageButton
            android:id="@+id/btn_refresh"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/menu_refresh"
            android:scaleType="centerInside"
            android:src="@drawable/ic_refresh" />

        <ImageButton
            android:id="@+id/btn_more"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/menu_more"
            android:scaleType="centerInside"
            android:src="@drawable/ic_more" />

        <ImageButton
            android:id="@+id/btn_collapse"
            android:layout_width="36dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/menu_collapse"
            android:scaleType="centerInside"
            android:src="@drawable/ic_collapse" />
    </LinearLayout>

    <!-- 收起后的小圆钮：点击展开浮条 -->
    <FrameLayout
        android:id="@+id/mini_fab"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="top|center_horizontal"
        android:background="@drawable/bg_mini_fab"
        android:elevation="6dp"
        android:visibility="gone">

        <ImageButton
            android:id="@+id/btn_expand"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/menu_expand"
            android:scaleType="centerInside"
            android:src="@drawable/ic_expand" />
    </FrameLayout>

</FrameLayout>